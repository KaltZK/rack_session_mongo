require 'spec_helper'

describe RackSessionMongo do
  it 'has a version number' do
    expect(RackSessionMongo::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
